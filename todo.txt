Security
  edit/new
  view drafts

RSS feed
  
Import data


GET /login/init
  create username & pwd from env variables
  if env variable is empty do nothing

GET /login/
POST /login/ 
  post values must match username and password in DB

GET /logout/
